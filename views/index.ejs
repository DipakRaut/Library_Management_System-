<h2 class="page-header">Recently Added Books 📖</h2>

<% books.forEach(items => { %>
<a href="/books/<%= items.id %> ">
  <img
    height="300"
    width="260"
    style="margin: 10px"
    src="<%= items.coverImagePath %> "
  />
</a>
<%}); %>
